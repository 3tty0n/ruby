rpython/jit/metainterp/virtualizable.py | 13 ++++++++++---
 rpython/rlib/jit.py                     | 11 +++++++++++
 2 files changed, 21 insertions(+), 3 deletions(-)

--- Changes ---

rpython/jit/metainterp/virtualizable.py
  @@ -2,6 +2,7 @@ from rpython.jit.codewriter.effectinfo import EffectInfo
  +from rpython.rlib.jit import gc_mark_state
   from rpython.rtyper import rvirtualizable
   from rpython.rtyper.lltypesystem import lltype, llmemory
   from rpython.rtyper.rclass import IR_IMMUTABLE_ARRAY, IR_IMMUTABLE
  @@ -219,7 +220,11 @@ class VirtualizableInfo(object):
  -                assert not virtualizable.vable_token
  +                # During a GC-mark read of a frame being traced the token
  +                # deliberately survives: the values are correct and the read
  +                # is not an escape.
  +                assert (not virtualizable.vable_token or
  +                        gc_mark_state.marking)
           self.clear_vable_token = clear_vable_token
   
           def tracing_before_residual_call(virtualizable):
  @@ -251,8 +256,10 @@ class VirtualizableInfo(object):
  -                # virtualizable escapes.
  -                virtualizable.vable_token = TOKEN_NONE
  +                # virtualizable escapes.  A read made only to mark GC roots
  +                # is not an escape, so it leaves the marker in place.
  +                if not gc_mark_state.marking:
  +                    virtualizable.vable_token = TOKEN_NONE
               else:
                   from rpython.jit.metainterp.compile import ResumeGuardForcedDescr
                   ResumeGuardForcedDescr.force_now(cpu, token)
  +10 -3

rpython/rlib/jit.py
  @@ -559,6 +559,17 @@ vref_None = non_virtual_ref(None)
  +class _GCMarkState(object):
  +    """Set by an embedder's GC mark hook around reading virtualizable fields:
  +    during tracing those fields are always correct, so a read that only marks
  +    must not clear vable_token and read as an escape."""
  +    def __init__(self):
  +        self.marking = False
  +
  +
  +gc_mark_state = _GCMarkState()
  +
  +
   ENABLE_ALL_OPTS = (
       'intbounds:rewrite:virtualize:string:pure:earlyforce:heap:unroll')
   
  +11 -0
